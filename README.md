
# Gemini Ghost Knowledge 👻

Интеллектуальный "призрачный" ассистент, который слушает вашу речь через микрофон и выводит глубокие контекстные подсказки в прозрачное окно поверх всех приложений. Идеально подходит для кодинга, проведения презентаций или обучения.

## Основные возможности
- **Live Mode**: Потоковая передача аудио в Gemini 2.5 Flash.
- **Detailed Intelligence**: Модель настроена давать развернутые ответы, а не короткие фразы.
- **Ghost Overlay**: Прозрачный режим (Overlay), который можно разместить поверх других окон.
- **Scrollable Interface**: Удобный скроллбар для чтения длинных текстов.
- **K8s Ready**: Готовые манифесты для деплоя в локальный кластер (Kind).

## Быстрый старт (Локально)

### 1. Предварительные требования
- **Node.js**: v18 или выше.
- **API Key**: Получите ключ в [Google AI Studio](https://aistudio.google.com/). 
  *Важно: Для стабильной работы Live API рекомендуется использовать проект с привязанным биллингом.*

### 2. Установка
```bash
# Клонируйте репозиторий
# Перейдите в папку проекта
npm install
```

### 3. Запуск
```bash
# Установите переменную окружения с вашим ключом
export API_KEY=ваш_ключ_здесь

# Запустите сервер разработки
npm start
```
Откройте [http://localhost:3000](http://localhost:3000). Нажмите "Initiate Listening" и разрешите доступ к микрофону.

---

## Запуск в Docker
```bash
# Сборка образа
docker build -t ghost-hint:latest .

# Запуск контейнера
docker run -p 8080:80 -e API_KEY=ваш_ключ ghost-hint:latest
```

---

## Деплой в Kubernetes (Kind)

Если вы используете локальный кластер `kind`:

1. **Создайте кластер** (если нет):
   ```bash
   kind create cluster --name gemini-lab
   ```

2. **Загрузите образ в Kind**:
   ```bash
   kind load docker-image ghost-hint:latest --name gemini-lab
   ```

3. **Примените манифесты**:
   ```bash
   kubectl apply -f k8s/deployment.yaml
   ```

4. **Проброс портов**:
   ```bash
   kubectl port-forward svc/ghost-hint-service 8080:80
   ```
Приложение будет доступно по адресу `http://localhost:8080`.

## CI/CD
В проекте настроен GitHub Actions (`.github/workflows/ci.yml`), который при каждом пуше проверяет сборку проекта и Docker-образа.
